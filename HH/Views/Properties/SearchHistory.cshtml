@model IEnumerable<HH.ViewModels.SearchHistoryViewModel>

@{
    var grid = new WebGrid();
}

   .webgrid-table {
        font-family: "Courier New", Courier, monospace;
        font-size: 12.5px;
        font-weight: normal;

        display: table;
        border-collapse: collapse;
        border: solid 1px #C5C5C5;
        background-color: white;

    }

         td, th {
            border: 1px solid #C5C5C5;
            padding: 3px 7px 2px;
            text-align: center;
        }

    .webgrid-header, .webgrid-header a {
        background-color: rgb(224, 72, 49);
        color: black;
        text-align: center;
        text-decoration: none;
        font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
    }

    .webgrid-footer {
    }

    .webgrid-row-style {
        padding: 3px 7px 2px;
    }

    .webgrid-alternating-row {
        background-color: #F5F5F5;
        padding: 3px 7px 2px;
    }

    .col1Width {
        width: 350px;
    }

    .col2Width {
        width: 230px;
    }

    .col4Width {
        width: 230px;
    }

<div id="gridContent" style="align-content:center; padding:20px; ">
    @grid.GetHtml(
                                tableStyle: "webgrid-table",
                                headerStyle: "webgrid-header",
                                footerStyle: "webgrid-footer",
                                alternatingRowStyle: "webgrid-alternating-row",
                                selectedRowStyle: "webgrid-selected-row",
                                rowStyle: "webgrid-row-style",
                                mode: WebGridPagerModes.All,
                                columns:
                                grid.Columns(
                                                grid.Column("Number", "Number", format: @<text>  <span class="display-mode"><label id="lblName">@item.number</label></span> </text>, style: "col1Width"),
                                                grid.Column("street", "street", format: @<text>  <span class="display-mode"> <label id="lblSKU">@item.street</label></span><span class="edit-mode"><label id="lblSKU"></label></span> </text>, style: "col2Width"),
                                                grid.Column("date", "date", format: @<text> <span class="display-mode"><label id="lblAlert">@item.date</label></span> </text>, style: "col4Width")
                                             )
                      )
</div>

<div>
    @Html.ActionLink("Housing Health Organization", "Index")
</div>


